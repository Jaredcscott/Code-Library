'''
    @Author Jared Scott ☯
    This function can be used to compute the value of a simple quadratic equation 
    Implements:
        x^2 + ax + b
'''

def computeQuadratic(a,b,x):
    result = (x ** 2) + (a * x) + b
    print(result)
    return result
